//int idRecuperado = 0;
//var add = context.Database.ExecuteSqlRaw($"MarcaAdd '{marca.NombreMarca}',@idRecuperado OUT", idRecuperado);
//context.Entry(marca).State = EntityState.Added;
//var add2 = context.Marcas.FromSql($"MarcaAdd '{marca.NombreMarca}'").AsEnumerable().FirstOrDefault();

@IdMarca INT
@IdSubMarca TINYINT 
@IdModeloSubMarca TINYINT
@IdCatalogoDescripcion INT

string ubicacionArchivo = "C:\\Users\\digis\\OneDrive\\Documents\\Aldair Santiago Garcia\\ProyectoGit\\ListaDeAutos.txt";
System.IO.StreamReader archivo = new System.IO.StreamReader(ubicacionArchivo);
int bandera=0;
string linea;
while ((linea = archivo.ReadLine()) != null)
{
    if(bandera==0)
    {
        bandera=1;
    }
    else
    {
        string[] words = linea.Split('\t');

        Console.WriteLine("Tenemos una línea:");
        Console.WriteLine(words);
    }
    
}
